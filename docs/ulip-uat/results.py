"""Executed test-case outcomes, recorded 11 August 2026 against ULIP staging
with the application running live against the whitelisted egress IP.

Kept as its own module so a re-run updates one table rather than the
narrative. Key is (ULIP api, case id); value is (status, actual result).
"""

RESULTS = {
    ('FASTAG/01', '01'): ('PASS',
     'HTTP 200. Three toll crossings returned for CG07BC9186 (Khireitangiri, Janasanpur and Khamara toll plazas, 21-22 May 2023) with timestamps, geocodes, lane direction and vehicle class VC12. Rows persisted and rendered on the FASTag screen.'),
    ('FASTAG/01', '02'): ('PASS',
     'HTTP 200 with zero crossings and an explicit empty state. No rows fabricated.'),
    ('FASTAG/01', '03'): ('PASS',
     'HTTP 400 returned by the application. No ULIP call was made.'),
    ('FASTAG/02', '01'): ('BLOCKED',
     'HTTP 200 with zero tags. NLDSL have confirmed that staging holds static test data and that tag details against a given vehicle number vary with the data configured there, so this path cannot be evidenced on staging. The tag-id path (TC-FASTAG02-02) exercises the same API successfully.'),
    ('FASTAG/02', '02'): ('PASS',
     'HTTP 200. One tag returned for the tag id, with TAGID, REGNUMBER, TID, VEHICLECLASS VC4, TAGSTATUS A (rendered as Active), ISSUEDATE, EXCCODE, BANKID and COMVEHICLE.'),
    ('FASTAG/02', '03'): ('PASS',
     'HTTP 400 returned by the application before any ULIP call, avoiding the documented respCode 239 failure.'),
    ('LDB/01', '01'): ('PASS',
     'HTTP 200 with decision_path LIVE and source ULIP. Thirteen movements returned for TCLU8538808 across vessel, rail and road legs - Gateway Terminals India (GTI), JNPT Central Parking Plaza and Khalapur toll plaza - each with coordinates, transport mode and timestamp, and each carrying its LDB milestone name (PORT IN, PORT OUT, GATE OUT). Events persisted to core.logistics_event and rendered on the Logistics Intelligence tile.'),
    ('LDB/01', '02'): ('PASS',
     "HTTP 200 with zero events. On staging LDB returns the same static trail (TCLU8538808) for every container number, including the sample NLDSL supplied; the application checks the trail against the container asked for and rejects a contradiction rather than filing another box's port milestones under it."),
    ('LDB/01', '03'): ('OBSERVED',
     "The reference is classified before dispatch: a value that is not a container number is treated as a vehicle reference and routed to FASTAG/01, returning an empty result rather than a validation error. Note that ULIP's own documented sample container <code>NSST1234570</code> does not carry an ISO 6346 category letter, so the classifier accepts any four letters followed by seven digits."),
    ('VAHAN/04', '01'): ('PASS',
     'HTTP 200 with <code>decision_path: LIVE_PRIMARY</code>. Full RC returned for UP32KH0320 - class, fuel, registration date, fitness to 2033-12-05, PUC, insurance, RTO and blacklist status CLEAR. Owner name arrives masked as documented.'),
    ('VAHAN/04', '02'): ('PASS',
     'HTTP 200. VAHAN/04 and VAHAN/01 were both attempted, neither returned the vehicle, and the ladder degraded to PROVISIONAL with a cure window. No partial record was created.'),
    ('VAHAN/04', '03'): ('PASS',
     'The verification appears in <code>core.vehicle_verification_history</code> with <code>source: LIVE_PRIMARY</code> and its timestamp, alongside the PROVISIONAL row from TC-VAHAN04-02.'),
    ('VAHAN/01', '01'): ('PASS',
     'HTTP 200. The XML payload parsed to the same record shape as VAHAN/04, so the retry is transparent to every consumer.'),
    ('VAHAN/01', '02'): ('PASS',
     'Both APIs were attempted exactly once and the ladder then degraded. NLDSL have confirmed that staging carries static test data whose responses need not reflect production, which explains why VAHAN/01 returned a different registration for the same input during testing; the application discards any answer whose registration number does not match the number queried, so a mismatch can never reach a gate decision.'),
    ('VAHAN/02', '01'): ('BLOCKED',
     'HTTP 404. VAHAN/04 masks the chassis number, and NLDSL have confirmed the masked fields fall under the PII clause and cannot be unmasked under the current grant. The endpoint itself responds correctly; a successful resolution cannot be evidenced without an unmasked key.'),
    ('VAHAN/02', '02'): ('PASS',
     'HTTP 404 with an explicit not-found. No empty record returned.'),
    ('VAHAN/03', '01'): ('BLOCKED',
     'HTTP 404, for the same reason as TC-VAHAN02-01 - the engine number is masked and NLDSL have confirmed masked fields fall under the PII clause.'),
    ('VAHAN/03', '02'): ('PASS',
     'HTTP 404 with an explicit not-found.'),
    ('SARATHI/02', '01'): ('PASS',
     'HTTP 200 with <code>decision_path: LIVE_PRIMARY</code>. Licence AP01620210000019 returned as Active with two classes of vehicle and validity to 2031-12-03. Row written to the DL lookup history with <code>source: LIVE_PRIMARY</code>.'),
    ('SARATHI/02', '02'): ('BLOCKED',
     'This licence returns an empty <code>TransportValidityTodate</code> on staging, so the non-transport date governs and the transport-preference cannot be demonstrated on live data. The behaviour is covered by an automated test against the documented contract.'),
    ('SARATHI/02', '03'): ('PASS',
     'HTTP 404. No driver record created; the lookup is recorded in the DL history as NOT_FOUND.'),
    ('SARATHI/02', '04'): ('BLOCKED',
     'No licence with a non-active status was available on staging. The fail-closed behaviour is covered by an automated test against the documented contract.'),
    ('SARATHI/01', '01'): ('PASS',
     'HTTP 200 with decision_path LIVE_PRIMARY, served by SARATHI/01 using the test data NLDSL supplied. The record carries the three fields SARATHI/02 does not: issue date 2012-03-07, issuing state Gujarat and RTO GJ33, alongside eight classes of vehicle and the transport validity. Note that the DL number must keep its internal space - <code>GJ04 20120005008</code> resolves and <code>GJ0420120005008</code> returns errorcd -1.'),
    ('SARATHI/01', '02'): ('PASS',
     "HTTP 422. The date of birth is validated against SARATHI/01's documented pattern at the application boundary, so a malformed value is rejected rather than silently downgrading the request to SARATHI/02."),
    ('GATISHAKTI/04', '01'): ('PASS',
     "HTTP 200. 59 toll plazas returned for Maharashtra and persisted to <code>core.gs_toll_plaza</code>, with coordinates converted from the response's string form to numeric values."),
    ('GATISHAKTI/04', '02'): ('PASS',
     'A second refresh returned the same 59 rows and the stored row count was unchanged - rows were updated in place, not duplicated.'),
    ('GATISHAKTI/04', '03'): ('PASS',
     'HTTP 200 with an empty row set and an explicit unavailable posture. Nothing written.'),
    ('GATISHAKTI/01', '01'): ('PASS',
     "HTTP 200. 33 rows returned for NH-5 and persisted. NLDSL have confirmed this API's response structure - road name, GIS length, road type, lane status and state/UT - and that it carries no coordinates."),
    ('GATISHAKTI/01', '02'): ('OBSERVED',
     'The read endpoint serves from the persisted reference set rather than calling ULIP per request, so an unrecognised NH number returns an empty result rather than a validation error. Client-side pattern validation applies on the refresh path, which is what calls ULIP.'),
    ('GATISHAKTI/02', '01'): ('PASS',
     'HTTP 200. 13 rows returned for state 27 and persisted. NLDSL have confirmed this API returns infrastructure data - name, address, ownership, storage capacity, type and village - and the application maps those fields as returned.'),
    ('GATISHAKTI/02', '02'): ('PASS',
     'HTTP 200 with an empty row set. Nothing written.'),
    ('GATISHAKTI/03', '01'): ('PASS',
     'HTTP 200. 994 rows returned for state 27 and persisted with coordinates. NLDSL have confirmed this API returns industrial land and park data - state, district, sub-district, village, park name, land category and availability, park type, latitude and longitude.'),
    ('GATISHAKTI/03', '02'): ('PASS',
     'Stored coordinates are numeric and retain full precision (for example 18.840293861130554, 73.07656771006351).'),
}
