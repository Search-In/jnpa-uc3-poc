"""Top-level service packages for the JNPA UC-III PoC.

Importable as ``services.<name>`` with the repo root on ``PYTHONPATH`` (the same
model the gateway relies on: compose bind-mounts the repo and sets
``PYTHONPATH=/app``).
"""
